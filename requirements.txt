# LocallyAI — full dependency union (core + ml).
#
# Convenience file: `pip install -r requirements.txt` gets you every
# dep the codebase can use. For CI / lean installs, use
# `requirements-core.txt` (no PyTorch / MLX). See those files for
# what's in each.

-r requirements-core.txt
-r requirements-ml.txt
