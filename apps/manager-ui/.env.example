# Manager UI environment template. Copy to .env.local for local development.
#
# Base URL of the LocallyAI backend (FastAPI server). The browser hits this
# URL directly, so it must be reachable from the operator's workstation and
# listed in the backend's LOCALLYAI_CORS_ORIGINS allowlist.
VITE_API_BASE_URL=http://localhost:8000
