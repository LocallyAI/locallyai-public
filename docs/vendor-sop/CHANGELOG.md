# Vendor SOP CHANGELOG

Append a one-line entry every time the Vendor SOP itself changes, OR
every time a vendor-side process change requires an SOP update. Newest
at the top.

Format:

    YYYY-MM-DD  <short description> [<commit-or-tag-ref>] (<author>)

The firm-side SOP CHANGELOG is at `docs/sop/CHANGELOG.md` — keep that
one separate (different audience, different read cadence).

---

## Entries

2026-05-11  Vendor SOP v1.2.2 — V8 vendor-onboarding §3.2 updated to reflect the single-use install command (firm SOP v3.12 shipped same day). Both bootstrap variants now backed by a one-time-use server-side token; replayed commands return HTTP 410. Vendor wins: leaked install commands cannot be used to register a malicious office Mac under a victim firm's identity. (<vendor-founder>)
2026-05-11  Vendor SOP v1.2.1 — V8 vendor-onboarding §3.2 updated for the new bootstrap one-liner (option A streamline shipped same day in firm SOP v3.10). Both paths documented (A = curl-piped bootstrap; B = manual git clone fallback) with explicit "when to prefer A vs B" guidance and GPG-verified variant of A for first installs at production firms. (<vendor-founder>)
2026-05-10  Vendor SOP v1.2 — added V13 vendor-internal-dryrun. Triggered by upcoming dogfood onboarding on cofounder's MacBook. Chapter codifies the pre-flight check before each "first" onboarding (first paying firm, first KSA firm, first HA fleet, first major install change, annual regression). Covers: trigger matrix (when a dry-run is justified), what dry-runs validate vs don't vs falsely reassure, two-device setup (cofounder Mac as office Mac, vendor laptop separate, optional staff laptop for LAN test), fake firm identity convention with date suffix to avoid hash collision across runs, per-phase walkthrough notes for the 7 phases, full cleanup checklist (cofounder-Mac side: launchctl unload, port check, optional install dir removal; vendor side: FIRM_TOKENS edit + push, firms-issued.log decommission row, archive firm record), lessons-learned template (vendor-records/dryruns/<YYYY-MM>-<scenario>.md), illustrative common pitfalls, anti-patterns to avoid, graduation criteria for "production-ready for first real firm". (<vendor-founder>)
2026-05-10  Vendor SOP v1.1 — added V8 vendor-onboarding (the vendor's deep playbook for taking a firm from prospect to live). Companion to docs/sop/onboarding.md (which is the cross-audience pipeline structure). New chapter covers: mental model (six "modes" the vendor cycles through), per-phase deep dives with vendor scripts + conversation templates + decision criteria, vendor laptop kit packing list, on-site install playbook (vendor's own checklist, not the firm-side install procedure), pre-departure checklist (don't leave site without these), engagement sign-off ceremony with the firm-profile.md sign-off block, common variations (KSA / HA fleets / pre-existing Mac / air-gapped firms), common onboarding failures + recovery (forgot to ingest corpus, DPA still not signed, IT contact left, telemetry not reaching dashboard), and onboarding metrics worth tracking for capacity planning. Renumbered V8-V11 to V9-V12 (customer success, sub-processors, compliance, people). build_sop_pdf.py CHAPTER_ORDER + pretty_title overrides updated. (<vendor-founder>)
2026-05-10  Vendor SOP v1.0 — initial. Master at docs/VENDOR_SOP.md plus 11 chapters in docs/vendor-sop/: vendor-team (roles, on-call rota, succession matrix, key custody, single-person operation risk callout), vendor-infrastructure (account inventory + secret tier matrix + backup locations + rotation cadence + discovery commands), vendor-daily-ops (start-of-shift checklist + weekly/monthly/quarterly/annual cadence + quiet-day backlog), vendor-release-engineering (cadence + pre-release checklist + GPG safety + bad-release rollback playbook), vendor-incidents-own-infra (when OUR infra breaks: laptop loss, GitHub takeover, CF takeover, GPG leak, TOTP leak, 1Password leak, Resend leak, telemetry-token leak, registrar takeover — each with kill-switch + rotation + comms + post-incident template), vendor-disaster-recovery (RTOs, sealed-envelope prerequisites, founder-incapacitated workflow, succession to designated firm vs source-code escrow vs wind-up), vendor-sales (qualification questions, demo structure, pricing, DPA process, handoff to onboarding), vendor-customer-success (cadence by firm age, weekly/monthly/quarterly templates, churn signals, per-firm CS log), vendor-sub-processors (HF/GitHub/CF/Resend/Slack/Anthropic/Apple/Microsoft inventory + what we deliberately don't use + adding a new sub-processor + annual review), vendor-compliance (vendor-side data inventory + RoPA + ISO 27001/SOC 2 readiness path + breach notification template + audit/inspection workflow + privacy-by-design commitments), vendor-people (hire criteria + onboarding ceremony with limited initial scope + 30-day and 90-day probation reviews + offboarding same-day cascade + termination for cause + garden leave + annual training). build_sop_pdf.py gains --vendor flag for separate PDF rendering. (<vendor-founder>)
