# SOP CHANGELOG

Append a one-line entry every time the SOP itself changes, OR every
time a code change requires an SOP update. Newest at the top.

Format:

    YYYY-MM-DD  <short description> [<commit-or-tag-ref>] (<author>)

If a code change should have triggered an SOP update but the SOP
wasn't touched, the next person to look at this file files it as a
backfill entry like:

    2026-MM-DD  BACKFILL: <commit-ref> introduced <feature/change>; updated <file> (<author>)

---

## Entries

2026-05-12  SOP v3.14: macOS version-lock + on-site install checklist. New chapter 4i (install-checklist.md) — tickable list for the engineer doing the on-site install: pre-arrival vendor laptop kit, firm-side phone check, on-arrival physical context, hardware sanity, version-lock step, bootstrap, TLS cert trust, launchd verification, telemetry round-trip, Workspace UI smoke, handover, 7-day soak, 30-day acceptance. Includes "common failures from V13 dogfood" cross-reference with all 11 commit refs. setup-mac-single.md gains §0.2a "Disable automatic macOS updates" (System Settings toggle path + Terminal verification + sw_vers recording). setup-mac-ha.md gains §0.0 covering both Macs (HA gives hardware redundancy, NOT software redundancy — both Macs must be on same version + build; upgrade lagging one before pairing). maintenance.md gains a top-of-file "macOS version policy" section: why (auto-update breaks .venv / MLX / launchd / mDNS / cert trust), supported version band table (must be updated each test cycle), vendor-side 10-step test procedure before approving a new version, firm-side upgrade procedure (manual only, vendor-approved, HA fleets stagger Mac-B then Mac-A with 24h gap), what firms do NOT do. (<vendor-founder>)
2026-05-11  SOP v3.13: telemetry token is auto-issued at form-submit time. Reported by user during dogfood: "cant the .md file be used to automatically generate the telemetry code on the client side". Yes, and now it is. New flow: form's "Generate install command" → POST /onboarding/mint-token → Worker decodes the intake blob, detects LOCALLYAI_TELEMETRY=1 + LOCALLYAI_FIRM_NAME, computes firm_id (matches scripts/onboard_firm.sh + form-side computation: SHA-256("locallyai-firm:<name>")[:16]), generates 32-byte hex token, registers firm_id → {token, firm_name, registered_at} in new TELEMETRY_TOKENS KV namespace (5-year TTL), appends LOCALLYAI_TELEMETRY_TOKEN= to the blob before storing. Bootstrap fetches blob via /onboarding/intake?t=<token>, writes .env including the token, no vendor manual step. /heartbeat firmAuth checks TELEMETRY_TOKENS KV first then falls back to FIRM_TOKENS env JSON for legacy backwards-compat (firms registered via onboard_firm.sh keep working). wrangler.toml gains a new [[kv_namespaces]] block for monitor_telemetry_tokens — operator runs `npx wrangler kv namespace create monitor_telemetry_tokens` once, pastes id, redeploys. Form's env-snippet comment updated: "auto-issued; no separate 1Password share or vendor-side step needed". onboarding.md §3 documents new default + legacy onboard_firm.sh path (rotation / air-gapped / audit). (<vendor-founder>)
2026-05-11  SOP v3.12: install command is now single-use. Previously the form inlined the intake blob into the curl URL; anyone who saw the command (email forward, scrollback, shared shell history) could replay it. Now the form POSTs the blob to a new monitor Worker endpoint POST /onboarding/mint-token, which stores the blob in a new INTAKE_TOKENS KV namespace (7-day TTL) and returns a 64-hex token. The install command becomes  curl -fsSL .../bootstrap | LOCALLYAI_INTAKE="$(curl -fsSL .../onboarding/intake?t=<token>)" bash  — the intake URL atomically marks the token consumed on first fetch and returns HTTP 410 on any subsequent fetch. Bootstrap script itself unchanged (still GPG-signable; LOCALLYAI_INTAKE comes from shell command substitution, not script body injection). Form UX: status pill "SINGLE-USE · 7-DAY EXPIRY" + one-paragraph explanation that leaked commands can't be replayed. setup-mac-single.md §0.0 + vendor-onboarding.md §3.2 updated. wrangler.toml adds the new INTAKE_TOKENS namespace block (operator must run npx wrangler kv namespace create monitor_intake_tokens once + paste the printed id, then redeploy). (<vendor-founder>)
2026-05-11  SOP v3.11: new chapter 4h remote-access.md covering remote-staff access options. The architecture's no-vendor-on-access-path posture is preserved across all four options: A (on-LAN only — default), B (Tailscale, free ≤100 devices, recommended), C (firm's existing corporate VPN), D (Cloudflare Tunnel + Access, firm's own CF account). For each: setup time, who manages, cost, attack surface, auditability, when to pick. Comparison table. Vendor's role explicitly bounded ("never on the tailnet / VPN / CF Access — firm-managed network plumbing, period"). Includes one-paragraph script for the onboarding call. Master SOP TOC + build_sop_pdf.py CHAPTER_ORDER + pretty_title overrides updated. client-install.md gains a top-of-chapter cross-link. Intake form gains a new "Remote-staff access plan" dropdown (A/B/C/D/TBD); answer flows into firm-profile.md under the Network section so the choice is captured at onboarding and survives staff turnover. (<vendor-founder>)
2026-05-11  SOP v3.10: install bootstrap one-liner (option A streamline). New static asset docs/monitor/cloudflare-worker/src/dashboard/bootstrap — bash script (~120 lines) that decodes a base64-encoded intake blob from env (LOCALLYAI_INTAKE), prompts interactively for the SSH deploy key (cat-paste) and telemetry token (read -s, hex-validated), sets up a temporary ssh-agent (deploy key never written to disk), pins github.com host key, clones LocallyAI/locallyai to ~/locallyai, writes .env (mode 0600) with the intake values + token, and exec's install.sh. The form gains a "Generate install command" button that base64-encodes the same env block the download path produces, composes the curl one-liner, and displays both the short unverified form and the GPG-verified form (fetch pubkey → bootstrap + .sig → gpg --verify → bash). Form fetches the bootstrap client-side and recomputes SHA-256 via crypto.subtle so the displayed fingerprint stays in sync with whatever's deployed (no hardcoded hash). New scripts/sign_bootstrap.sh — detached ASCII-armored GPG signature with local verification before exit. setup-mac-single.md §0.0 + vendor-onboarding.md §3.2 both updated to document path A (bootstrap) vs path B (manual git clone) with explicit "when to prefer A vs B" guidance. (<vendor-founder>)
2026-05-10  SOP v3.9.2: onboarding chapter rewritten as the master end-to-end pipeline. Old chapter (intake → registration only, ~200 lines) replaced with full 9-phase runbook (phase 0 pre-engagement → phase 8 steady-state + annual review). Each phase has owner, wall-clock estimate, links to per-step chapter (setup-mac-single, client-install, daily, bulk-ingest, etc.), and acceptance criteria. Includes copy-pasteable email templates (intake invitation, token-share with credential warning, 30-day leave-behind). Common variations documented inline: KSA firms (insert phase 3a setup-saudi.md, swap DPA template), HA fleets (use setup-mac-ha.md instead), pre-existing Mac (add --pre-flight audit step), air-gapped firms (skip phase 2.3, add fortnightly check-in calls). Top-level "fully onboarded" checklist as the engagement close-out criterion. Wall-clock from LOI to "users productive": typically 3-5 working days. (<vendor-founder>)
2026-05-10  SOP v3.9.1: onboarding registration tooling. New private repo LocallyAI/vendor-records with skeleton (firms/, dpas/, firms-issued.log audit log, README explaining what does + does NOT belong there, .gitignore blocking *token*/*secret*/*.key/.env at commit time). New scripts/onboard_firm.sh — one command consumes a firm-profile-*.md: parses firm_id + H1 name, refuses to proceed silently if recomputed hash mismatches declared (catches typos/tampering), generates fresh 32-byte hex token via openssl, merges into ~/.locallyai/vendor/firms-registry.json (0600 local source of truth — wrangler can't read secrets back), pushes merged FIRM_TOKENS via npx wrangler secret put, appends audit row to firms-issued.log (no token values logged), prints token for operator to share via 1Password/PGP. Re-running on registered firm rotates with confirmation. SOP onboarding.md updated to point at the new repo + script. (<vendor-founder>)
2026-05-10  SOP v3.9: vendor onboarding intake form. New static page at docs/monitor/cloudflare-worker/src/dashboard/onboarding.html — public URL (no auth, since firm IT fills it out before they have any keys), pure client-side: live SHA-256(firm_name)[:16] firm_id computation, generates two downloads (firm-profile-<slug>.md for vendor records + install-<slug>.env paste-ready for office Mac). Three priority tiers (must / should / nice) with field-level rationale visible in the UI so firm IT understands why each field matters. New SOP chapter 4g (onboarding.md): vendor workflow for sending the URL, verifying the returned hash, filing the profile in vendor-records repo, registering the firm_id+token in the monitor Worker FIRM_TOKENS secret, sending the right DPA (UK / KSA), and calendaring the 12-month re-confirm. setup-mac-single.md gains a new step 0.0 noting the .env can be pre-staged from the intake. vendor-monitoring.md cross-links to onboarding.md. PDF chapter order + title overrides updated. (<vendor-founder>)

2026-05-10  SOP v3.8: vendor fleet-monitoring dashboard with 4-hour SLA + self-heal inventory. New telemetry.py — firm-side opt-in agent posting 5-min anonymised heartbeats (firm_id = SHA-256(firm_name)[:16], no document content / user names / queries / audit content; only health gauges + structured alert codes). New docs/monitor/cloudflare-worker/ — TOTP-gated CF Worker (POST /heartbeat firm-token-auth, GET /api/firms + /api/alerts admin-TOTP, POST /api/ack). KV: FIRM_STATE (7d expiry) + ALERTS (30d). Cron every 15min checks unacknowledged critical alerts >3.5h old → re-notify as SLA escalation. Notification routing via Resend (free 3000/mo email) + optional Slack webhook. Static dashboard served from src/dashboard/ (CF Workers Assets binding) — TOTP login, firm grid with red/yellow/green/grey cards, alert table with SLA-at-risk highlighting, ack workflow. New self-healers in watchdog/sentinel.py: healthz_kickstart (API down → launchctl kickstart), ollama_restart (llama-runner-terminated → pkill + relaunch), disk_pressure_clean (<5GB → aggressive log+GC). Each emits telemetry self_heal event (success → info; failed → critical alert). install.sh fleet path adds explicit "Enable vendor health telemetry? [y/N]" with full disclosure (default off). New SOP chapters 4e (data-isolation.md gains telemetry section disclosing exactly what's sent + opt-out) and 4f (vendor-monitoring.md — vendor-internal on-call playbook, 4h SLA workflow, alert code → diagnostic mapping, self-heal inventory, account-separation requirements). PDF regenerated. (<vendor-founder>)
2026-05-10  SOP v3.7.1: kill-switch incident runbook fully rewritten in updates.md (was still referencing the old GitHub-backed AWS S3 design from before the CF Worker move). New structure: pre-requisites, decision tree, step-by-step per operation (stop / blocklist / require-version / resume), expected-output verification, lost-phone recovery, comms templates for affected firms, post-incident checklist. Cross-linked from incidents-software.md and incidents-security.md as a banner so on-call operators land on the runbook from any incident-response entry point. (<vendor-founder>)
2026-05-10  SOP v3.7: data-isolation surface — UI affirmation, SOP chapter, egress allowlist. New /v1/branding endpoint (unauthenticated) returns firm_name + office_host + isolation_statement read from new LOCALLYAI_FIRM_NAME env. install.sh prompts for firm display name (auto-derives from office hostname if skipped). Worker UI: "Firm: <name>" pill in main header + LoginGate; isolation statement under the cert-status strip on the gate. Manager UI: same pill in TopBar across every page + LoginGate. Both UIs degrade gracefully if /v1/branding is unreachable (older deployments). New SOP chapter 4e (data-isolation.md): exhaustive cross-firm threat model with diagram, every-network-call table, compliance mapping (GDPR Art. 5/32, ISO 27001 A.5.34/A.8.10/A.8.20/A.8.22, KSA PDPL Art. 22). New docs/egress-allowlist/ with README (per-host purpose), lulu-rules.json (one-click LuLu import — Objective-See free firewall), pf-conf option for single-purpose Macs. New scripts/audit_egress.sh — pgrep-tight process selection + CIDR-aware allowlist check; PASS/FAIL summary. Fleet-mode end-of-install summary calls out the egress story + LuLu install hint. (<vendor-founder>)
2026-05-10  SOP v3.6.3: kill-switch Worker rewritten to use Cloudflare KV (drop GitHub entirely). Previous design: Worker held a GitHub PAT, wrote status.json to a separate locallyai-status GitHub account. New design: Worker reads/writes its own CF KV namespace; office Macs poll the Worker URL directly. Removes the second-GitHub-account requirement (no more "create another email" friction) and removes the GITHUB_PAT secret. Threat model improvement: zero GitHub surface area for the kill switch — compromising the LocallyAI org doesn't reach it at all. Free-tier coverage: 100k requests/day Workers + 1k/day KV reads (60s server-side cache means actual KV reads are halved); comfortably free up to ~50 firms. Migration: existing GH-backed deployments keep working via the legacy fallback in scripts/kill_switch_emergency.sh, but new deploys should use KV. README walks through CF account creation (any email — Outlook, ProtonMail, iCloud all free), wrangler login, KV namespace creation, secret upload, deploy. install.sh default LOCALLYAI_KILL_SWITCH_URL now blank with a comment explaining per-deployment setup. (<vendor-founder>)
2026-05-10  SOP v3.6.2: TOTP-gated kill switch via Cloudflare Worker. New docs/kill-switch/cloudflare-worker/ — TypeScript Worker (~250 lines, pure Web Crypto, no deps) holds the GitHub PAT + TOTP secret server-side. Operator's laptop holds nothing sensitive — only the Worker URL. Auth = 6-digit TOTP from phone authenticator (Google Authenticator / 1Password / Authy / iOS Passwords / Bitwarden) via RFC 6238 + 10 single-use recovery codes (sha256-hashed in Worker env). New scripts/kill_switch_totp_setup.sh — generates 160-bit base32 secret + recovery codes + otpauth:// URI (with ASCII QR via brew install qrencode); never persists secret to disk. Updated scripts/kill_switch_emergency.sh — picks API mode when LOCALLYAI_KILL_SWITCH_API_URL set (prompts for TOTP via read -s, POSTs to Worker), falls back to direct-gh path otherwise. Deployment via wrangler (CF free tier — supports 69 firms before paid plan needed). Threat-model improvement: previously laptop compromise = kill switch compromise; now needs (TOTP code OR recovery code) AND (Worker URL) to flip — credential separation across phone + CF account. Recovery: lost phone → use a recovery code, then regenerate the pool. (<vendor-founder>)
2026-05-10  SOP v3.6.1: out-of-band kill-switch operationalised. install.sh's .env block now sets LOCALLYAI_KILL_SWITCH_URL by default to https://raw.githubusercontent.com/locallyai-status/locallyai-status/main/status.json — points at a SEPARATE GitHub account (NOT the LocallyAI org) so credential compromise of one doesn't reach the other. Required=1 by default (fail-closed). New scripts/kill_switch_emergency.sh — operator quick actions: stop|resume|blocklist|require-version|status. Refuses to run if gh is authed as the LocallyAI account (would defeat the OOB defence) — must be the locallyai-status account. Reads current JSON, shows diff, confirms, publishes via GitHub Contents API. Firms react within ≤60s (kill_switch.py cache TTL). docs/kill-switch/status.json.template — initial empty payload to commit on first setup. updates.md gains the kill-switch quick-action section + tiered host options (separate-GH-account → CF Pages → S3+CF). (<vendor-founder>)
2026-05-10  SOP v3.6: full tiered system-update path with 7-layer defence-in-depth. Two channels (dev → 24h soak → stable; LOCALLYAI_UPDATE_CHANNEL env), GPG-signed tags (git verify-tag against docs/release-signing-key.gpg), SHA-256 manifest per release (release_manifest.json with artifacts[].sha256, manifest hashes verified via git show <tag>:<file>), OOB kill switch (kill_switch.py polls a static JSON on a host SEPARATE from GitHub — defends against full GitHub-account compromise), per-tier opt-out (LOCALLYAI_AUTO_UPDATE=off pins to manual; LOCALLYAI_AUTO_UPDATE_TIERS=A,B controls auto-apply), atomic deploy with healthz-poll auto-rollback (deploy.py — checkout, restart, 60s /healthz wait, revert on fail), HMAC-chained audit entries for every started/applied/rolled-back/refused. Sentinel auto-applies tier A only. Manager UI: new /updates route with chain-of-trust strip + per-update verification verdicts + Apply button gated by all-checks-pass; new /models route with curated MLX picker (Qwen 2.5 1.5B/3B/7B/14B, Llama 3.2 3B, Mistral Small) — selecting downloads via huggingface-hub, atomically swaps MLX_MODEL in .env, restarts API. Vendor-side: scripts/release_server.sh — `dev VERSION TIER SUMMARY` builds manifest + signed tag; `promote VERSION` enforces 24h soak before re-tagging as stable. New SOP chapter 4d (updates.md) — vendor procedures, firm-side procedures, incident playbook for bad-release scenarios, future-work list (code-signing, reproducible builds, canary cohort, branch protection). PDF regenerated. (<vendor-founder>)
2026-05-10  SOP v3.5.2: office-server distribution of client installers — IT downloads .dmg/.msi from the firm's own Mac, not from GitHub. Code-side: client_installers.py (gh CLI shells out to LocallyAI/locallyai's -clients releases, caches under storage/installers/, atomic-promote tmp dir to live, path-traversal hardened resolve_file); api.py admin endpoints GET /admin/installers (list + status), POST /admin/installers/refresh (background pull), GET /admin/installers/{filename} (FileResponse stream behind admin auth). watchdog/sentinel.py _refresh_client_installers tick — daily best-effort pull, silent if gh missing. Manager UI gains /downloads route with sidebar "Client Apps" entry — status strip (last tag, last pull, gh-availability warning), grouped grid (Worker × Mac/Win, Manager × Mac/Win), per-file Download button using authedFetch+blob (admin key never appears in URL), Check-for-updates button. install.sh fleet path adds optional "Set up installer mirror? [Y/n]" — installs gh via brew if absent, walks operator through gh auth login, kicks off first pull in background. SOP client-install.md updated: office-server path is now primary, GitHub Releases listed as fallback. (<vendor-founder>)
2026-05-10  SOP v3.5.1: install.sh now wires fleet-mode end-to-end. New "Choose fleet topology" picker (single = loopback only, fleet = LAN-bound for client apps); on fleet pick the installer auto-sets LOCALLYAI_BIND=0.0.0.0, detects the office Mac's mDNS hostname (scutil --get LocalHostName) + LAN IP (ipconfig getifaddr en0/en1), adds both to LOCALLYAI_CORS_ORIGINS along with the Tauri webview origins, and stamps LOCALLYAI_OFFICE_HOST. End-of-install summary prints the URL staff laptops should use, the GitHub Releases URL where IT downloads the installers, and the recommended NEXT STEPS (firewall the subnet, pre-stage the URL on staff laptops via MDM, push the .dmg/.msi). FLEET_TOPOLOGY env override for unattended installs. Also new scripts/release_clients.sh — one command (auto-bumps patch, updates both Tauri configs, commits, tags vX.Y.Z-clients, pushes) to trigger the GitHub Actions build that produces installers attached to the release page. client-install.md gains the install-time prerequisite section + the new release command. (<vendor-founder>)
2026-05-10  SOP v3.5: standalone Tauri client apps (LocallyAI Worker + Manager). New apps/clients/{worker,manager}-tauri Tauri 2 projects — thin native shells (~10 MB installer) that open a config screen on first launch, save the office server URL to the OS app-config dir, then load <server>/worker (or /manager) in the bundled webview. No Python / npm / MLX / Qdrant on client devices. Builds via GitHub Actions on tag push (.github/workflows/build-clients.yml — matrix macOS+Windows × worker+manager → .dmg / .app for Mac, .msi / .exe for Windows; uploads to the GitHub Release). Unsigned (one-time right-click-Open or "More info → Run anyway" per device; signing flagged as a future cert spend). New SOP chapter 4c (client-install.md): Mac drag-to-Applications, Windows msiexec, Jamf/Munki/Intune/Group Policy bulk push instructions including a config-plant script that pre-stages the server URL so end users skip the prompt. Server-side: scripts/start_locallyai.sh now honours LOCALLYAI_BIND (default 127.0.0.1; set to 0.0.0.0 to serve LAN client devices); api.py CORS allowlist includes Tauri webview origins (tauri://localhost on macOS, https://tauri.localhost on Windows). Security note added re. LAN exposure + recommended subnet firewall rule (ISO 27001 A.8.20). (<vendor-founder>)
2026-05-09  SOP v3.4.3: KSA mixed Arabic/English corpus is verified end-to-end. setup-saudi.md updated with the LLM-side picker (MLX Qwen2.5 3B/7B/14B in 4-bit; Ollama qwen2.5:7b/14b with the caveat that Ollama ≤0.22.1 has a Metal `half vs bfloat` static_assert bug on recent macOS that prevents *any* model from loading; Llama-3.2-* explicitly NOT recommended — no official Arabic). Code-side: api.py RELEVANCE_FLOOR 0.02 → 0.01 (commit 54556d3) so RRF top-1 cross-lingual hits — score 1/(k+1)=0.0164 with k=60, just below the old floor — pass through; Arabic queries against English corpora otherwise returned 0 sources because BM25 contributes nothing and only the multilingual vector ranks the chunk. Demo state captured: backend=mlx, MLX_MODEL=mlx-community/Qwen2.5-3B-Instruct-4bit, EMBED_MODEL=intfloat/multilingual-e5-base, LOCALLYAI_DATA_REGION=KSA, QDRANT_URL commented out (embedded store at storage/ — single-Mac deployments don't need a Qdrant server, embedded mode survives reboots). (<vendor-founder>)
2026-05-09  SOP v3.4.2: split one-click launcher into per-UI apps. LocallyAI.app + start_locallyai.vbs replaced by LocallyAI Worker.app/start_worker.vbs (API+worker-ui only) and LocallyAI Manager.app/start_manager.vbs (API+manager-ui only). Each only loads the LaunchAgent it needs (so worker-only users don't pay the manager-ui memory cost). Stop LocallyAI.app still kills all three. Removed the auto-open of https://localhost:8000/healthz on first HTTPS launch — was meant to ease cert acceptance but was annoying in practice; documented as a manual one-time step instead. Stop script: dropped `set -u` (empty array iteration tripped on bash 3.2 mid-script and prevented the port-sweep fallback from running, leaving stragglers); added a 1s settle after launchctl bootout so the port-sweep doesn't race a still-dying pid. SOP one-click-start.md rewritten for the per-UI model. (<vendor-founder>)
2026-05-09  SOP v3.4.1: launcher TLS-aware. start_locallyai.sh detects tls/{cert,key}.pem and starts uvicorn with --ssl-keyfile/--ssl-certfile so the API scheme matches what the rest of the stack expects (LOCALLYAI_API_BASE=https, worker-ui .env.local=https). Manager-ui .env.local was http (mismatched) → now https. CORS allowlist in .env was missing the manager-ui port (5173) → added. Launcher's readiness probe uses curl -k to accept the self-signed cert. On first run with HTTPS the launcher opens https://localhost:8000/healthz first so the browser shows the cert-acceptance prompt before the UI silently fails. SOP one-click-start.md updated with the cert-accept step + new troubleshooting entries (TLS scheme mismatch, CORS allowlist mismatch). LOCALLYAI_ALLOW_HTTP=1 documented as the HTTP-fallback escape hatch. (<vendor-founder>)
2026-05-09  SOP v3.4: one-click launcher rewritten to use macOS launchd LaunchAgents (KeepAlive=true) so services stay warm between clicks. First click writes 3 plists to ~/Library/LaunchAgents/ and bootstraps them (~1.6s to all-3-serving-200); subsequent clicks just open browser tabs (~250ms). Stop boots them out and deletes the plists. Browser tabs now open in parallel — each tab fires the moment its specific port is reachable rather than waiting for all three. nohup fallback path retained for Linux. Updated one-click-start.md (cold/warm timing table + launchctl inspection commands + auto-start guidance) and stop-locallyai.sh tree-walk fallback. (<vendor-founder>)
2026-05-08  SOP v3.3: added one-click-start.md (chapter 4b). Tracks new code: scripts/start_locallyai.sh + stop_locallyai.sh (background launcher with PID-file, log dir, health-port wait, browser-tab open, idempotent restart, recursive process-tree kill); LocallyAI.app + Stop LocallyAI.app macOS bundles (LSUIElement=true so no Dock icon flicker; resolves repo via app-relative path or ~/Library/Application Support/LocallyAI/repo_path override); start_locallyai.vbs + stop_locallyai.vbs Windows wrappers (hidden cmd, run scripts/start_locallyai.bat + stop_locallyai.bat which use PowerShell Start-Process for detached background launches). Master TOC + CHAPTER_ORDER updated. (<vendor-founder>)
2026-05-08  SOP v3.2: added bulk-ingest.md (frictionless gigabyte-scale corpus loading via the manager UI; chunked/resumable upload protocol; 2-worker indexing pool with batched BM25 rebuild). Tracks code-side: api.py /v1/uploads/* + /v1/ingest/status + /v1/ingest/flush endpoints, chunked_uploads.py append-to-disk + sha256 verify + GC, ingest_queue.py 30s-quiet batched rebuild, sentinel _gc_stale_uploads tick. Worker-ui: chunked use-uploads + folder picker + pause/resume/cancel + IngestStatusTicker. Manager-ui: rewritten /documents page with same affordances + live library refresh from GET /v1/documents. Master TOC updated; CHAPTER_ORDER for the PDF builder updated. (<vendor-founder>)
2026-05-06  SOP v3.1: added repo-access.md (vendor-side SSH deploy-key procedure: per-client ed25519 key, GitHub deploy-key registration read-only, SSH config alias, lifecycle incl. rotation/revocation, HA per-node keys, troubleshooting). Cross-linked from setup-mac-single.md §2 and setup-windows.md §1. Master TOC + CHANGELOG + build_sop_pdf.py CHAPTER_ORDER updated. PDF regenerated. (<vendor-founder>)
2026-05-06  SOP v3 / Saudi (PDPL) variant: added setup-saudi.md and compliance-saudi.md. Master index updated. Tracks code-side Saudi work: LOCALLYAI_DATA_REGION env + audit/billing region stamping, install picker (mandatory pick), conditional embed-model default + TLS cert C=, breach-detector citation per region, system-prompt Arabic-aware suffix, RoPA bumped to v1.3 with applicable_regulations / breach_notification per region, DPA_DRAFT_SA.md (PDPL fresh draft, not GDPR translation), 6 Saudi demo docs, RTL+i18n scaffolding across worker-ui/manager-ui/fleet-ui (en+ar JSON dictionaries; logical-property Tailwind sweep; Hijri/SAR formatters). Tag v0.sa1. (<vendor-founder>)
2026-05-06  SOP v2: added incidents-{people,legal,misuse,service,supply}.md and scale-out.md (~50 new scenarios). Expanded incidents-physical.md with heat/cooling, voltage spike, lockdown, construction. Expanded incidents-operator.md with sudo'd-destructive-command, git reset, evidence-pack-misdelivery, time-zone confusion. Master index updated. PDF builder added at scripts/build_sop_pdf.sh (Chrome-headless pipeline; falls back to pandoc if installed). (<vendor-founder>)
2026-05-06  Initial SOP set: master index, setup-mac-{single,ha}, setup-windows, daily, maintenance, compliance, incidents-{software,physical,security,operator}, recovery, decommission, this CHANGELOG. Reflects repo state as of v0.ha-phase10. (<vendor-founder>)
